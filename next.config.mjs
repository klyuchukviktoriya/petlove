/** @type {import('next').NextConfig} */
export const nextConfig = {
  images: {
    domains: [
      "ftp.goit.study",
      "www.nytimes.com",
      "media4.giphy.com",
      "media.giphy.com",
    ],
  },
};

export default nextConfig;
